program test;

var o : integer;
	p : real;
	q : string;
	r, s : boolean;
	t : char;

procedure x(a : integer; b : real; c : string; d, e : boolean; f : char);
begin
	a := 1;
	b := 23.5;
	c := 'hello world!';
	d := true;
	e := false;
	f := 'X'
end;

begin
	x(o, p, q, r, s, t)
end.
